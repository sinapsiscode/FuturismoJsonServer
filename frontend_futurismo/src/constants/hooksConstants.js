/**
 * Constantes compartidas para todos los hooks
 */

// Delays comunes para simulación de API
export const API_DELAYS = {
  FAST: 500,
  NORMAL: 1000,
  SLOW: 2000
};

// Estados de carga comunes
export const LOADING_STATES = {
  IDLE: 'idle',
  LOADING: 'loading',
  SUCCESS: 'success',
  ERROR: 'error'
};

// Timeouts comunes
export const TIMEOUTS = {
  DEBOUNCE: 300,
  THROTTLE: 100,
  AUTO_SAVE: 2000,
  NOTIFICATION: 3000
};